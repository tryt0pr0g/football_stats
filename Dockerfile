# Используем официальный образ Python как базовый
FROM python:3.11-slim

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файл зависимостей
COPY requirements.txt .

# Устанавливаем зависимости
RUN pip install --no-cache-dir -r requirements.txt

# Копируем все остальное содержимое проекта в контейнер
COPY . .

# Указываем, какой порт использует приложение (для API-сервиса)
EXPOSE 8000

# Команда по умолчанию для запуска Uvicorn (это будет использоваться для ОСНОВНОГО СЕРВИСА)
# Задание по умолчанию: запуск API, которое включает APScheduler (ежедневное обновление)
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--workers", "4"]

# ----------------------------------------------------------------------------------
# ВАЖНО: Этот же образ будет использоваться для CLOUD RUN JOB,
# но с другим ENTRYPOINT, который будет выглядеть так:
# ENTRYPOINT ["python", "run_job.py"]
# ----------------------------------------------------------------------------------